import linker
import permutation
def convolution(winSize,polList,inputBits):
	window = inputBits[:winSize]
	while winSize <= len(inputBits) :
		
		for polynomial in polyList :
			poly = bin(polynomial)[2:]
		
						
	
